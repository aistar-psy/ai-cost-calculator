# Google Analytics 配置指南

## 📊 使用统计功能说明

本工具已集成 Google Analytics 4 (GA4) 统计功能，可以追踪：
- **访问人数**：有多少人访问了工具
- **使用次数**：总共进行了多少次成本测算
- **使用的模型**：哪些模型被使用最多
- **测算金额分布**：不同规模项目的分布情况
- **历史记录查看**：用户查看历史记录的频率
- **管理员登录**：后台配置模式的使用情况

## 🚀 快速配置步骤

### 第一步：创建 Google Analytics 账号

1. 访问 https://analytics.google.com/
2. 使用 Google 账号登录
3. 点击"开始衡量"创建新账号

### 第二步：创建媒体资源

1. 输入媒体资源名称：`AI成本测算工具`
2. 选择时区：`中国`
3. 选择货币：`人民币 (CNY)`
4. 点击"下一步"

### 第三步：设置数据流

1. 选择平台：`网站`
2. 输入网站网址：`https://aistar-psy.github.io`
3. 输入数据流名称：`成本测算工具`
4. 点击"创建数据流"

### 第四步：获取 Measurement ID

创建完成后，你会看到一个 **Measurement ID**，格式类似：`G-XXXXXXXXXX`

**复制这个 ID，你需要用它来替换代码中的占位符。**

### 第五步：更新代码

1. 编辑 `/Users/xingkong/cost-calculator/index.html`
2. 找到这两处 `G-XXXXXXXXXX`：
   ```html
   <script async src="https://www.googletagmanager.com/gtag/js?id=G-XXXXXXXXXX"></script>
   ```
   和
   ```javascript
   gtag('config', 'G-XXXXXXXXXX');
   ```
3. 将 `G-XXXXXXXXXX` 替换为你的真实 Measurement ID
4. 保存文件

### 第六步：部署更新

```bash
cd /Users/xingkong/cost-calculator
git add index.html
git commit -m "添加 Google Analytics 统计功能"
git push
```

等待 1-2 分钟后，统计功能就会生效。

## 📈 查看统计数据

### 实时数据
1. 登录 Google Analytics
2. 左侧菜单选择"报告" → "实时"
3. 可以看到当前正在使用工具的人数

### 历史数据
1. 左侧菜单选择"报告" → "参与度" → "事件"
2. 可以看到所有追踪的事件：
   - `cost_calculation` - 成本测算次数
   - `view_history` - 查看历史记录次数
   - `admin_login` - 管理员登录次数

### 自定义报告
1. 左侧菜单选择"探索"
2. 创建自定义报告，可以按：
   - 日期范围
   - 使用的模型
   - 测算金额范围
   - 地域分布

## 🎯 追踪的事件详情

### 1. 成本测算事件 (cost_calculation)
**触发时机**：用户点击"开始测算"按钮

**追踪参数**：
- `event_category`: "calculation"
- `event_label`: 使用的模型名称（如 "doubao-seed-2.0-mini-128k"）
- `value`: 测算的年度成本（万元）
- `total_users`: 学生总人数
- `model`: 模型名称

**用途**：
- 统计总测算次数
- 分析哪些模型最受欢迎
- 了解项目规模分布

### 2. 查看历史记录事件 (view_history)
**触发时机**：用户点击侧边栏的历史记录

**追踪参数**：
- `event_category`: "history"
- `event_label`: 项目名称
- `value`: 该项目的年度成本（万元）

**用途**：
- 统计历史记录查看频率
- 了解用户是否经常回顾之前的测算

### 3. 管理员登录事件 (admin_login)
**触发时机**：管理员成功输入密码进入后台配置模式

**追踪参数**：
- `event_category`: "admin"
- `event_label`: "enter_admin_mode"

**用途**：
- 监控后台配置的使用频率
- 了解是否需要调整配置权限

## 📊 常用统计指标

### 基础指标
- **总访问量**：有多少人访问了工具
- **独立访客数**：去重后的用户数
- **平均会话时长**：用户平均使用时长
- **跳出率**：打开后立即离开的比例

### 自定义指标
- **测算次数**：`cost_calculation` 事件总数
- **人均测算次数**：测算次数 / 独立访客数
- **最受欢迎的模型**：按 `event_label` 分组统计
- **平均测算金额**：`value` 参数的平均值

## 🔒 隐私说明

Google Analytics 会收集以下信息：
- 访问时间
- 地理位置（城市级别）
- 设备类型（桌面/移动）
- 浏览器类型
- 用户操作（测算、查看历史等）

**不会收集**：
- 具体的项目名称（除非用户主动填写）
- 详细的测算参数
- 用户的个人身份信息

## 🛠️ 故障排查

### 问题1：看不到数据
**解决方案**：
1. 确认 Measurement ID 已正确替换
2. 等待 24-48 小时（GA4 有数据延迟）
3. 使用"实时"报告查看即时数据
4. 检查浏览器是否安装了广告拦截插件

### 问题2：事件没有追踪
**解决方案**：
1. 打开浏览器开发者工具（F12）
2. 切换到 Network 标签
3. 筛选 "gtag" 或 "analytics"
4. 执行操作，查看是否有请求发送

### 问题3：数据不准确
**解决方案**：
1. 检查是否有多个 GA 代码片段
2. 确认代码放在 `<head>` 标签内
3. 清除浏览器缓存后重试

## 📱 移动端统计

GA4 会自动识别移动端访问，你可以在报告中看到：
- 移动端 vs 桌面端的使用比例
- 不同设备的使用体验差异
- 移动端的跳出率

## 🎓 进阶功能

### 设置转化目标
1. 进入 GA4 管理界面
2. 选择"事件" → "创建事件"
3. 将 `cost_calculation` 标记为转化事件
4. 可以追踪"测算完成率"

### 设置用户细分
1. 创建受众群体
2. 按使用频率分组：
   - 新用户（首次访问）
   - 活跃用户（多次测算）
   - 高价值用户（测算大额项目）

### 导出数据
1. 在报告页面点击"导出"
2. 支持导出为 PDF、CSV、Google Sheets
3. 可以定期生成报告发送给团队

## 📞 需要帮助？

如果配置过程中遇到问题：
1. 查看 Google Analytics 帮助中心：https://support.google.com/analytics
2. 检查浏览器控制台是否有错误信息
3. 确认网站已成功部署到 GitHub Pages

## 🔄 更新记录

- **2026-05-11**: 初始版本，添加基础统计功能
  - 成本测算事件追踪
  - 历史记录查看追踪
  - 管理员登录追踪
